# 정보 중요도 및 혼잡도 기반 다중 형식 AER 통신망 설계 제안서

> 제안명: **CARE-AER(혼잡 적응형 복원 고려 AER)**  
> 문서 상태: 설계 제안 v0.2  
> 대상: 2026 AI반도체회로설계경진대회 디지털 설계 1차 과제 및 2차 확장

---

## 0. 개요

### 0.1 제안 주제

> **정보 중요도와 통신망 혼잡도를 고려한 다중 형식 적응형 AER 통신 및 소프트웨어 이벤트 복원 구조 설계**

### 0.2 한 문장 요약

비동기적으로 발생하는 극성 이벤트를 항상 같은 형식으로 보내지 않는다. 지역별 이벤트 밀도, 극성 일치도, 시간적 반복성과 FIFO 혼잡도를 확인해 `RAW`, `MASK`, `BIN` 중 하나를 선택한다. 비닝 크기도 2×2와 4×4 중에서 고른다. 중요한 이벤트는 원본 그대로 보내거나 위치를 보존하는 비트맵으로 묶고, 반복성이 높은 이벤트만 개수 중심으로 줄인다. 후단 소프트웨어는 현재 토큰 하나만 보지 않고 최근 여러 시간 구간의 정확한 이벤트, 비닝 크기, 움직임을 함께 이용해 줄어든 공간 정보를 복원한다.

### 문서에서 계속 사용하는 기술 용어

| 용어 | 쉬운 뜻 |
|---|---|
| AER | 이벤트가 생긴 픽셀의 주소를 보내는 통신 방식 |
| FIFO | 먼저 들어온 이벤트부터 내보내는 임시 저장소 |
| RAW | 이벤트를 좌표·극성·시각과 함께 하나씩 보내는 형식 |
| MASK | 2×2 또는 4×4 영역에서 이벤트가 생긴 위치를 비트맵으로 묶어 보내는 형식 |
| BIN | 일정 영역의 정확한 위치 대신 ON/OFF 개수를 보내는 요약 형식 |
| SYNC | 여러 패킷이 같은 시간 기준을 쓰도록 맞추는 형식 |
| 토큰 | 통신망으로 보내는 한 묶음의 정보. 대부분 32비트 한 단위이며 4×4 MASK만 두 단위를 이어서 사용함 |
| RTL | 실제 디지털 회로로 합성할 수 있도록 동작을 기술한 설계 코드 |
| PPA | 성능, 소비전력, 회로 면적을 함께 평가하는 지표 |
| `ready/valid` | 받을 준비가 되었는지와 보낼 데이터가 유효한지를 알리는 흐름 제어 신호 |

### 0.3 설계 목표

1. 이벤트가 드물 때는 좌표·극성·시간 정보를 정확하고 빠르게 전달한다.
2. 이벤트가 많거나 순간적으로 몰릴 때는 인접 이벤트의 좌표와 시간 정보 중복을 줄여 이벤트당 전송 비트 수와 패킷 수를 줄인다.
3. 중요한 집중 영역은 높은 해상도로 유지하고, 그 안의 반복 정보만 선택적으로 줄인다.
4. 짧은 순간 폭주는 FIFO에 임시 저장한다. 과부하가 오래 지속될 때는 무작위 손실 대신 정해진 규칙에 따라 품질을 단계적으로 낮춘다.
5. 부호기, 패킷 생성기, FIFO, 통신망, 복호기를 RTL로 구현하고 45 nm 교육용 공정에서 성능·전력·면적(PPA)을 평가한다.
6. 줄인 토큰을 최근 여러 시간 구간의 정보와 함께 소프트웨어로 복원해 전송량과 복원 오차의 관계를 수치로 비교한다.
7. 1차 출력이 2차의 센서 좌표-월드 좌표 변환기에 바로 연결되도록 입출력 형식을 정한다.

### 0.4 비목표

다음 항목은 초기 핵심 범위에서 제외한다.

- 포토다이오드와 아날로그 이벤트 발생 픽셀 자체의 설계
- 밝기 영상 복원
- PCIe/CXL/UCIe와 호환되는 물리 계층(PHY) 구현
- 라우터 내부의 대규모 신경망 또는 학습 회로
- `BIN` 토큰으로 원래 이벤트를 완벽하게 복원한다는 주장
- 이벤트 발생 시각을 클록보다 더 잘게 추정하는 시간 초해상도 기능
- 움직이는 물체와 센서 움직임을 동시에 분리하는 완전한 위치 추정·지도 작성(SLAM)

### 0.5 제안 기여점

본 설계의 기여점 후보는 다음과 같다.

1. **정보 중요도를 고려한 전송 형식 선택**  
   단순히 이벤트 수만 보지 않는다. 이벤트 밀도, 극성 일치도, 이전 패턴과의 차이를 이용해 정보의 반복성과 복잡도를 추정한다.

2. **혼잡도에 따른 다중 전송 형식**  
   지역 FIFO 사용량과 다음 라우터의 정지 상태에 따라 RAW, MASK, BIN 형식을 바꾼다.

3. **중요 이벤트 보존**  
   혼잡한 타일을 무조건 낮은 해상도로 만들지 않는다. 새롭거나 복잡한 패턴은 RAW 또는 MASK로 보존하고, 반복성이 높은 패턴만 BIN으로 줄인다.

4. **복원을 고려한 토큰**  
   BIN 토큰에 기준 좌표, ON/OFF 개수, 시간 구간, 묶음 크기를 담는다. 소프트웨어는 이 값을 지키면서 이벤트 위치를 추정한다.

5. **하드웨어-소프트웨어 공동 설계**  
   RTL에서 나온 실제 토큰을 소프트웨어 복원기에 넣는다. 전송량, PPA, 복원 오차, 월드맵 오차를 처음부터 끝까지 한 흐름으로 평가한다.

6. **시간에 따라 달라지는 비닝을 복원 단서로 사용**  
   같은 영역이 이전에는 RAW나 MASK로 정확히 전송되고 현재만 BIN으로 줄었을 수 있다. 소프트웨어는 최근의 정확한 위치와 특징의 이동을 현재 복원에 사용한다. 반대로 정확한 정보가 너무 오래되면 하드웨어가 MASK를 다시 보내 복원 기준을 갱신한다.

### 0.6 제안 기준 사양

아래 값은 공식 조건이 아니라 첫 구현을 위한 제안값이다. 기준 구조를 측정한 뒤 조정한다.

| 항목 | 최소 기능 검증 | 기준 구현 | 확장 분석 |
|---|---:|---:|---:|
| 센서 배열 | 16×16 또는 32×32 | 128×128 | 256×256 이상으로 크기를 바꾸어 분석 |
| 픽셀 타일 | 4×4 또는 8×8 | 16×16 | 같은 타일 반복 |
| 공간 묶음 | 2×2 | 2×2/4×4 가변 선택 | 4×4 선택 기준 비교 |
| 통신망 | 2×2 격자망 | 8×8 타일 격자망 | 여러 출구 비교 |
| 전송 단위 폭 | 32비트 | 32비트 | 16/64비트 민감도 분석 |
| 초기 클록 | 100 MHz | 100 MHz 고정 비교 + 최대 주파수 | 배치·배선 결과 기반 |
| 타일 지역 FIFO | 4-8개 | 8개 | 깊이별 비교 |
| 라우터 입력 FIFO | 2개 | 2-4개 | 면적·지연 비교 |
| 시간 구간 | 8클록 | 8 또는 16클록 | 구간 길이별 비교 |
| 전송 형식 | RAW/MASK/BIN/SYNC | 동일 | 패턴 형식 추가 가능 |
| 소프트웨어 복원 | 현재와 과거 4개 구간 | 과거 4-8개 구간 | 과거 구간 수와 움직임 정렬 방식 비교 |

---

## 1. 기존 연구

### 1.1 이벤트 센서와 AER

이벤트 카메라의 각 픽셀은 밝기 변화가 기준값을 넘을 때 비동기적으로 이벤트를 만든다. 기본 이벤트는 다음과 같이 표현한다.

\[
e_i=(x_i,y_i,t_i,p_i)
\]

- `x, y`: 센서 안의 픽셀 좌표
- `t`: 이벤트가 발생한 시각
- `p`: 극성. 밝아지면 ON, 어두워지면 OFF

주소 이벤트 표현(AER)은 이벤트를 만든 픽셀의 주소와 부가 정보를 공유 통신선으로 전달하는 방식이다. 여러 픽셀이 동시에 전송을 요청하면 중재기가 순서를 정한다. 선택된 이벤트는 주소 부호화와 요청·응답 절차를 거쳐 출력된다.

전통적인 기준 구조는 다음 순서로 동작한다.

```text
픽셀 이벤트
→ 이벤트 저장 래치
→ 행 전송 요청
→ 행 중재기
→ 행 래치
→ 열 중재기
→ 주소 부호기
→ (x, y, 극성)
→ 확인 응답 및 초기화
```

### 1.2 AER 구조의 발전

| 구조 | 핵심 개념 | 장점 | 남는 문제 |
|---|---|---|---|
| 전체 주소 병렬 AER | 전체 주소를 공유 버스로 전송 | 단순하고 이벤트 단위가 명확함 | 주소와 요청·응답 반복, 중앙 중재 병목 |
| 주소 순차·연속 전송 | 공통 행 주소를 한 번 보내고 열 주소를 연속 전송 | 같은 지역 이벤트의 주소 반복 감소 | 묶음을 기다리는 시간과 시간 오차 가능 |
| 그룹 AER | 인접 픽셀 여러 개를 비트맵으로 표현 | 이벤트가 많거나 몰릴 때 처리량 개선 | 이벤트가 적을 때 비트맵 낭비, 고정된 묶음 크기 |
| 순차 주사 | 정해진 순서로 픽셀 상태를 읽음 | 중재가 단순하고 출력 순서가 일정함 | 이벤트가 적을 때 비효율적이고 픽셀별 읽는 시각이 다름 |
| 계층형 토큰 링 | 트리와 링 구조를 계층적으로 결합 | 이벤트가 적을 때와 많을 때의 절충 | 제어가 복잡하고 입력 유형마다 최적 구조가 다름 |
| 설정 가능한 AER | 이벤트 패턴에 따라 부호화 구조를 변경 | 환경 변화에 적응 가능 | 설정 기능으로 면적과 제어 비용 증가 |

로컬 논문 정리에는 Boahen의 초기 AER, Culurciello의 행·열 중재, Son의 그룹 AER, Purohit와 Manohar의 계층형·설정 가능 부호기가 주요 흐름으로 정리되어 있다.[^bg-summary]

### 1.3 기존 묶음 처리와 이벤트 초해상도 복원

픽셀 비닝 또는 풀링은 여러 인접 픽셀을 하나의 낮은 해상도 표현으로 합쳐 데이터량을 줄인다. 이벤트 영역에서는 다음 값을 합칠 수 있다.

- 이벤트 존재 여부
- ON/OFF 이벤트 개수
- ON을 +1, OFF를 -1로 본 합계
- 가장 최근 발생 시각
- 이벤트가 발생한 위치 비트맵

고정형 풀링은 구현이 간단하다. 하지만 중요한 영역과 중요하지 않은 영역을 같은 비율로 줄인다는 문제가 있다. 또한 개수만 남기면 원래 이벤트가 어느 픽셀에서 발생했는지 정확히 알 수 없다.

이벤트 초해상도 연구는 낮은 해상도 이벤트의 공간·시간 관계를 학습해 높은 해상도 이벤트를 추정할 수 있음을 보였다. ICCV 2021 연구는 공간과 시간의 제약을 함께 학습했고, CVPR 2024의 BMCNet은 ON과 OFF 이벤트를 나누어 처리하면서 서로의 정보를 교환했다.[^esr-2021][^bmcnet-2024]

이 아이디어는 모자이크 제거와 비슷한 면이 있다. 모자이크 제거는 한 위치에서 빠진 정보를 주변 표본으로 채운다. 본 제안은 여기에 시간축을 더한다. 현재 4×4 BIN 때문에 위치 정보가 부족하더라도, 이전 시간 구간에 같은 영역이 RAW나 MASK로 전송되었다면 그 정확한 위치를 단서로 쓸 수 있다. 특징이 이동했을 수 있으므로 과거 지도를 그대로 복사하지 않고 먼저 현재 위치에 맞춰 정렬해야 한다.

차이점도 분명하다. 일반적인 모자이크 무늬는 고정되어 있지만, 본 설계의 빠진 위치는 혼잡도와 이벤트 특성에 따라 계속 바뀐다. 또한 이벤트는 희소하고 비동기적이며 ON/OFF 극성을 가지므로, 비닝 형식과 마지막 정확 정보의 나이를 복원기에 함께 알려야 한다.

### 1.4 기존 접근의 공백

기존 연구는 각각 다음 문제를 해결한다.

- AER 중재와 주소 부호화
- 연속 전송·그룹·순차 주사를 통한 처리량 개선
- 설정 가능한 부호기를 통한 입력 변화 대응
- 고정형 풀링을 통한 입력 해상도 축소
- 소프트웨어 초해상도를 통한 낮은 해상도 이벤트 복원

본 제안에서 집중하는 공백은 다음 연결이다.

> **통신망 혼잡도와 지역 이벤트 특성에 따라 2×2/4×4 비닝을 실시간으로 고르고, 최근의 정확한 표본과 움직임을 이용하면서 하드웨어 토큰의 이벤트 개수·극성·묶음 크기를 지켜 복원하는 전체 구조**

이는 현재 확인한 자료 범위에서의 차별점 후보이다. 최종 제출 전에는 내용 인지형 AER, 혼잡 인지형 신경모사 통신망, 적응형 이벤트 압축, 이벤트 초해상도 분야를 추가 조사해야 한다.

### 1.5 과제 조건과의 연결

Q&A에서 확인된 조건은 다음과 같다.[^qa]

- 기본 데이터는 밝기값이 아닌 극성 이벤트이다.
- 버스 폭, 패킷, 시간 정보, 요청·응답, 중재, 손실 정책은 팀이 정할 수 있다.
- 이벤트 손실이 곧바로 기능 실패를 뜻하지는 않는다. 하지만 처리량·지연·손실·PPA의 관계를 수치로 보여야 한다.
- 이벤트가 드문 조건, 많은 조건, 순간적으로 몰리는 조건을 팀이 정의해야 한다.
- 픽셀별 좌표를 생략한 자체 패킷과 복호기를 설계할 수 있다.
- 동기식 RTL 구현이 허용된다.
- 부호기뿐 아니라 복호기와 전체 입출력 경로까지 평가하는 것이 권장된다.
- 1차 출력은 2차 센서 좌표-월드 좌표 변환과 칩 내부 SRAM 기록으로 연결되어야 한다.

---

## 2. 문제 정의

### 2.1 이벤트 입력 특성

이벤트 입력은 시간과 공간에 따라 크게 변한다.

1. **희소 입력**: 소수의 픽셀에서 드물게 이벤트가 발생한다.
2. **집중 입력**: 특정 타일 또는 관심 영역에 이벤트가 집중된다.
3. **순간 폭주**: 짧은 시간 동안 순간 입력률이 출력률보다 높아진다.
4. **고밀도 입력**: 배열 전체 또는 넓은 영역에서 높은 이벤트율이 지속된다.
5. **연속된 윤곽**: 인접 픽셀에서 같은 극성 이벤트가 비슷한 시간에 발생한다.
6. **복잡한 세부 정보**: ON/OFF가 섞이고 패턴 변화가 커 정보량이 높다.
7. **잡음과 반복**: 특정 픽셀의 고립·반복 이벤트가 통신량을 차지한다.

### 2.2 처리 용량과 손실 조건

시간 구간 `t`에서 들어온 이벤트 수를 `A(t)`, 내보낼 수 있는 이벤트 수를 `S(t)`, FIFO에 쌓인 수를 `Q(t)`, FIFO 크기를 `B`라고 하면 다음과 같이 표현할 수 있다.

\[
Q(t+1)=\min\left(B,\max(0,Q(t)+A(t)-S(t))\right)
\]

FIFO 용량을 넘어 손실되는 이벤트 수는 다음과 같다.

\[
L(t)=\max(0,Q(t)+A(t)-S(t)-B)
\]

- 순간적으로 `A(t) > S(t)`여도 폭주가 짧고 FIFO가 충분하면 손실이 발생하지 않는다.
- 장시간 평균 입력률이 출력률보다 높으면 유한 FIFO만으로는 손실을 방지할 수 없다.
- 과부하가 오래 지속되면 통신선 폭 증가, 이벤트 묶음 전송, 표현 축약 또는 선택적 손실이 필요하다.

### 2.3 핵심 문제: 이벤트가 많다고 항상 중요한 것은 아니다

단순히 혼잡한 영역을 풀링하면 다음 문제가 생긴다.

```text
이벤트가 많은 영역
→ 실제로는 빠르게 움직이는 중요한 물체일 수 있음
→ 무조건 해상도를 낮추면 중요한 정보를 손실함
```

따라서 본 설계는 이벤트 밀도뿐 아니라 정보의 반복성과 복잡도를 함께 본다.

| 이벤트 밀도 | 정보 복잡도·패턴 변화 | 의미 | 처리 방법 |
|---|---|---|---|
| 낮음 | 낮음/높음 | 전송량이 작음 | RAW로 정확 전송 |
| 높음 | 높음 | 중요한 윤곽·복잡한 움직임 가능 | RAW 또는 MASK 우선 전송 |
| 높음 | 낮음 | 같은 극성과 비슷한 패턴이 반복됨 | MASK 또는 BIN |
| 고립 반복 | 낮음 | 잡음 후보 | 낮은 우선순위 또는 선택적 제거 |

### 2.4 설계 목적 함수

본 설계는 단일 지표만 최소화하지 않고 다음 목표를 함께 다룬다.

1. 이벤트당 평균 전송 비트 수 최소화
2. 지속 가능한 처리량 최대화
3. 평균 지연과 하위 99% 지연 최소화
4. 안전 입력률 이하에서 내부 이벤트 손실 방지
5. 과부하에서 중요한 이벤트 보존률 최대화
6. 복원 F1 점수와 월드맵 정확도 최대화
7. 부호기·복호기·라우터 면적과 이벤트당 에너지 최소화

전송량과 복원 오차의 관계는 다음과 같이 평가한다.

\[
R = \frac{\text{전송한 비트 수}}{\text{입력 이벤트 수}}
\]

\[
D = 1-\text{복원 F1 점수}
\]

같은 `R`에서 `D`가 작거나, 같은 `D`에서 `R`이 작은 구조가 우수하다.

### 2.5 입력 및 시스템 가정

초기 RTL에서는 다음을 가정한다.

- 픽셀별 병렬 전송 요청(`req`)과 극성(`polarity`) 신호를 타일 단위로 입력받는다.
- 픽셀 요청은 해당 픽셀이 확인되거나 저장될 때까지 1로 유지된다.
- 비동기 요청은 입력 경계에서 동기화하고, 내부 통신망은 하나의 클록으로 동작한다.
- 기본 극성은 ON/OFF 1비트이다.
- 전체에서 공유하는 시간 계수기를 사용하고 패킷에는 기준 시각과의 차이를 넣는다.
- 최소 기능 검증은 2×2로 시작하고, 기준 구현은 같은 수집기에서 2×2와 4×4를 선택한다.
- 같은 시간 구간에서 동일 픽셀·동일 극성이 반복되면 하나의 발생으로 합친다. 원 입력 수와 합쳐진 수는 별도 통계 계수기에 기록한다.
- 중요한 이벤트인지는 학습 모델이 아니라 간단한 하드웨어 계산으로 판단한다.
- `BIN`은 근사 표현이다. 이벤트 개수는 보존하지만 정확한 위치와 발생 시각은 보장하지 않는다.
- 각 4×4 영역은 마지막으로 정확한 RAW/MASK 정보를 보낸 뒤 지난 시간 구간 수를 기록한다. 이 값이 기준을 넘으면 BIN 대신 MASK를 보내 복원 기준을 갱신한다.

---

## 3. 아이디어

### 3.1 전체 구조

```text
비동기 픽셀 요청
          ↓
입력 동기화 회로 / 이벤트 래치
          ↓
4×4 이벤트 수집기
  - 내부를 네 개의 2×2 영역으로 나눔
          ↓
정보 특성 계산기
  - 이벤트 밀도
  - 극성 일치도
  - 이전 패턴과의 차이
          ↓
혼잡도 기반 전송 형식 제어기
          ↓
┌─────────┼──────────┐
│ RAW     │ MASK     │ BIN
│ 개별    │ 위치     │ 개수
│ 이벤트  │ 보존     │ 보존
└─────────┼──────────┘
          ↓
32비트 패킷 생성기 / 타일 FIFO
          ↓
타일 격자형 통신망
          ↓
출구 / 하드웨어 토큰 복호기
          ↓
움직임 정렬·개수 제약형 시계열 복원기
          ↓
복원된 이벤트
          ↓
월드 좌표 변환 / SRAM
```

### 3.2 이벤트 정보 특성 계산

현재 4×4 수집 영역에서 이벤트가 발생한 위치를 `M_t`, 이전 시간 구간의 발생 위치를 `M_{t-1}`라고 한다. 수집기는 16비트 위치 지도를 만들고, 필요하면 이를 네 개의 2×2 위치 지도로 나누어 계산한다.

#### 이벤트 밀도

\[
D=\operatorname{popcount}(M_t)
\]

`popcount`는 비트맵에서 1의 개수를 세는 연산이다. 이를 이용하면 몇 개의 픽셀에서 이벤트가 발생했는지 알 수 있다. 2×2는 최대 4개, 4×4는 최대 16개이므로 묶음 크기마다 밀도 기준을 따로 둔다. 같은 시간 구간에서 동일 픽셀·동일 극성이 반복되면 발생 위치는 한 번만 세고, 반복 횟수는 성능 통계용 계수기에 기록한다.

#### 극성 일치도

ON 개수를 `N_{ON}`, OFF 개수를 `N_{OFF}`라고 한다. 나눗셈 없이 다음 조건을 만족하면 한쪽 극성이 대부분이라고 판단한다.

```text
N_ON ≥ K × N_OFF
또는
N_OFF ≥ K × N_ON
```

초기 `K`는 3으로 두고 여러 기준값을 비교해 조정한다.

#### 이전 패턴과의 차이

\[
V=\operatorname{popcount}(M_t\oplus M_{t-1})
\]

- `V`가 크면 이전과 다른 새로운 패턴이다.
- `V`가 작고 같은 극성이 반복되면 정보를 합쳐도 될 가능성이 높다.

#### 마지막 정확 정보의 나이

각 4×4 영역에 마지막으로 위치를 정확히 보낸 시간 구간 번호를 저장하고, 현재 번호와의 차이를 `exact_age`라고 한다.

- 해당 영역의 모든 이벤트를 RAW 또는 MASK로 빠짐없이 전송하면 마지막 정확 시간 구간 번호를 갱신한다.
- 일부 긴급 이벤트만 RAW로 우회하고 나머지를 BIN으로 보낸 경우에는 영역 전체가 정확하지 않으므로 갱신하지 않는다.
- 모든 영역의 계수기를 매번 증가시키지 않고, 활성 영역을 처리할 때 `현재 구간 번호-마지막 정확 구간 번호`를 계산한다.
- `exact_age`가 기준값을 넘으면 혼잡하더라도 MASK를 한 번 보내 복원 기준을 갱신한다.
- 초기 기준값은 4개 시간 구간으로 두고 1, 2, 4, 8을 비교한다.

이 규칙은 소프트웨어가 너무 오래된 위치만 믿고 잘못 복원하는 것을 막는다. 영상 압축의 기준 화면과 비슷한 역할이지만, 여기서는 영역마다 독립적으로 갱신한다.

#### 혼잡도

지역 FIFO 사용량과 다음 라우터의 `ready` 정지 상태를 이용해 2비트 혼잡 상태를 만든다.

```text
00: 여유
01: 보통
10: 혼잡
11: 매우 혼잡
```

전송 형식이 매 순간 바뀌는 현상을 막기 위해 혼잡 상태에 들어가는 기준과 빠져나오는 기준을 다르게 둔다. 이를 히스테리시스라고 한다.

### 3.3 전송 형식

#### 형식 0: RAW(개별 이벤트)

개별 이벤트의 좌표, 극성, 기준 시각과의 차이를 전송한다.

- 이벤트가 적을 때 적합
- 가장 높은 시간·공간 정확도
- 이벤트마다 주소를 반복하므로 이벤트가 많을 때는 비효율적

#### 형식 1: MASK(위치 비트맵)

2×2 또는 4×4 영역의 기준 좌표와 ON/OFF 위치 비트맵을 전송한다.

- 픽셀 위치와 극성 발생 여부를 복원 가능
- 묶음 내부 좌표를 반복하지 않음
- 하나의 시간 구간을 공유하므로 개별 발생 시각의 정밀도는 낮아짐
- 같은 픽셀의 같은 극성이 시간 구간 안에서 여러 번 발생하면 횟수는 보존하지 않을 수 있음
- 2×2 MASK는 32비트 전송 단위 하나, 4×4 MASK는 머리부와 32비트 비트맵 본문으로 구성된 전송 단위 두 개를 사용

#### 형식 2: BIN(개수 요약)

2×2 또는 4×4 영역의 ON/OFF 개수와 대표 시각을 전송한다.

- 위치를 완전히 보존하지 않음
- 패킷 수와 주소 반복이 가장 적음
- 변화가 작고 반복이 많은 영역 또는 과부하 상태에서 사용
- 소프트웨어 복원 대상

#### 형식 3: SYNC(시간 동기화·설정)

시간 기준값, 설정값 또는 중간 확인 정보를 전달한다.

- 시간 차이 값이 표현 범위를 넘는 문제 방지
- 부호기와 복호기의 시간 기준 동기화
- 필요하면 패킷 손실 통계와 설정 갱신에 사용

### 3.4 전송 형식 선택 규칙

첫 구현은 학습 회로를 사용하지 않는다. 비교기와 1의 개수를 세는 회로만으로 판단한다.

| 혼잡 상태 | 이벤트 특성 | 마지막 정확 정보 | 선택 형식과 크기 |
|---|---|---|---|
| 여유 | 이벤트 1개 | 관계없음 | RAW |
| 여유 | 이벤트 2개 이상 또는 패턴이 복잡함 | 관계없음 | 2×2 MASK 또는 4×4 MASK |
| 보통 | 2×2 안에서 반복성이 높음 | 최근 정보 있음 | 2×2 BIN |
| 보통 | 4×4 전체가 비슷하고 안정적임 | 최근 정보 있음 | 4×4 BIN |
| 혼잡 | 패턴 변화가 크거나 ON/OFF가 섞임 | 관계없음 | 높은 우선순위의 2×2 MASK |
| 혼잡 | 한쪽 극성이 많고 4×4 패턴 변화가 작음 | 최근 정보 있음 | 4×4 BIN |
| 매우 혼잡 | 중요하거나 오래 기다린 이벤트 | 관계없음 | RAW 또는 높은 우선순위의 MASK |
| 모든 상태 | `exact_age`가 기준을 넘음 | 오래됨 | MASK를 보내 기준 정보 갱신 |

핵심 원칙은 다음과 같다.

> 혼잡한 영역의 해상도를 무조건 낮추지 않고, 혼잡한 영역 안에서 반복성이 높은 정보만 줄인다.

4×4 BIN은 압축 효과가 큰 대신 위치 손실도 크다. 따라서 단순히 이벤트 수가 많다는 이유만으로 선택하지 않는다. 4×4 안의 패턴 변화가 작고, 극성이 비교적 일정하며, 최근 정확 정보가 남아 있을 때만 사용한다. 조건을 만족하지 않으면 네 개의 2×2 영역으로 나누어 각각 처리한다.

### 3.5 32비트 전송 단위 제안

128×128 센서를 기준으로 `x`, `y`는 각각 7비트를 사용한다. 통신망은 항상 32비트씩 전달한다. RAW, 2×2 MASK, BIN, SYNC는 한 단위를 사용하고, 4×4 MASK는 머리부 한 단위와 비트맵 본문 한 단위를 연속으로 사용한다.

#### RAW 패킷

```text
31       30 29        23 22        16 15 14         7 6          0
+-----------+------------+------------+--+-------------+------------+
| type=00   | x[6:0]     | y[6:0]     | p| delta_t[7:0]| seq/meta   |
+-----------+------------+------------+--+-------------+------------+
```

- `type`은 전송 형식을 구분하는 2비트 값이다.
- `x`, `y`는 픽셀 좌표이고 `p`는 ON/OFF 극성이다.
- `delta_t`는 기준 시각에서 얼마나 지난 뒤 발생했는지를 나타낸다.
- `seq/meta`는 패킷 순서 확인이나 부가 상태 표시에 쓰는 7비트 예비 필드이다.

#### 2×2 MASK 패킷

```text
31       30 29 28        22 21        15 14       8 7    4 3      0
+-----------+--+------------+------------+-----------+------+--------+
| type=01   |S0| base_x     | base_y     |delta_t[6:0]|ONmask|OFFmask|
+-----------+--+------------+------------+-----------+------+--------+
```

- `S=0`은 2×2 MASK를 뜻한다.
- `base_x`, `base_y`는 2×2 영역의 기준 좌표이다.
- `ONmask`, `OFFmask`는 각각 4비트이다.
- 동일 픽셀에서 ON과 OFF가 모두 관측되면 두 비트맵에 동시에 표시할 수 있다.
- 개별 발생 순서와 정밀한 발생 시각은 보존하지 않는다.

#### 4×4 MASK 패킷

4×4는 ON/OFF 위치만으로 32비트가 필요하므로 두 번에 나누어 보낸다.

```text
머리부 32비트
31       30 29 28    24 23    19 18       12 11    7 6       0
+-----------+--+--------+--------+-----------+--------+---------+
| type=01   |S1|block_x |block_y |delta_t[6:0]|seq[4:0]|meta[6:0]|
+-----------+--+--------+--------+-----------+--------+---------+

바로 뒤의 본문 32비트
31                         16 15                           0
+----------------------------+-----------------------------+
| ONmask[15:0]               | OFFmask[15:0]               |
+----------------------------+-----------------------------+
```

- `S=1`은 4×4 MASK 머리부를 뜻한다.
- `block_x`, `block_y`는 128×128 센서를 4×4로 나눈 영역 번호이므로 각각 5비트이다.
- 라우터는 머리부를 보낸 뒤 바로 다음 본문까지 같은 출력 경로를 유지한다. 두 전송 단위 사이에 다른 패킷을 끼워 넣지 않는다.
- 복호기는 `seq`를 이용해 머리부와 본문 짝이 맞는지 확인한다.
- 4×4 MASK는 총 64비트이므로 이벤트가 1-2개뿐일 때는 이득이 없다. 대체로 3개 이상이면서 위치를 보존할 가치가 있을 때 사용한다.

#### BIN 패킷

```text
31       30 29 28        22 21        15 14    10 9      5 4       0
+-----------+--+------------+------------+--------+--------+---------+
| type=10   |S | base_x     | base_y     |ON[4:0] |OFF[4:0]|dt[4:0]  |
+-----------+--+------------+------------+--------+--------+---------+
```

- `S=0`은 2×2, `S=1`은 4×4 BIN을 뜻한다.
- `base_x`, `base_y`는 영역 왼쪽 위 픽셀의 좌표이며 묶음 크기에 맞게 정렬한다.
- `ON`, `OFF`는 해당 시간 구간에서 각 극성이 관측된 고유 픽셀 수이다. 5비트로 2×2의 0-4와 4×4의 0-16을 모두 표현한다.
- `dt[4:0]`은 BIN의 대표 시간 차이이다.

#### SYNC 패킷

```text
31       30 29                                            0
+-----------+----------------------------------------------+
| type=11   | timestamp_epoch / control payload[29:0]      |
+-----------+----------------------------------------------+
```

- `timestamp_epoch`은 이후 패킷이 공통으로 사용할 기준 시각이다.
- `control payload`는 초기화나 설정값 전달을 위한 제어 정보이다.
- 위 영어 필드명은 실제 RTL 신호명과 비트 배치를 분명히 하기 위해 유지했다.

SYNC는 가장 짧은 BIN 시간 차이 필드를 기준으로 32클록이 지나기 전 또는 값이 넘치기 전에 전송한다. 실제 주기는 추가 패킷 비용과 시간 오차를 비교해 정한다.

### 3.6 격자형 통신망

각 타일은 픽셀 수집기, 부호기, 지역 FIFO, 5방향 라우터에 연결된다.

```text
             북
             │
서쪽 ─── 라우터 ─── 동쪽
             │
             남
             │
           지역 타일
```

- 32비트 전송 단위
- 지역·북·남·동·서의 5개 입출력
- 첫 구현은 경로가 고정된 XY 전달 방식
- `ready/valid`를 이용한 역압 제어
- 순환 우선순위 중재와 최대 대기시간 기준
- 라우터 입력 FIFO 2-4개
- 지역 FIFO 사용량을 전송 형식 제어기에 되먹임
- 4×4 MASK의 머리부를 선택하면 바로 다음 본문까지 같은 출력 포트를 잠그는 두 단위 전송 기능

격자망 자체를 핵심 차별점으로 주장하지 않는다. 센서 크기가 커졌을 때 지역 배선으로 확장하기 위한 통신 구조로 사용한다. 출구가 하나라면 출구 주변 병목이 남으므로 다음 두 경우를 따로 평가한다.

1. **총 출력 대역폭 고정**: 중앙 버스·트리·격자망을 같은 총 출력 대역폭에서 비교
2. **출구 수 확장**: 2개 또는 4개 출구와 여러 좌표 변환기·SRAM 묶음을 사용해 확장성 분석

### 3.7 시간축 적응형 이벤트 복원

#### 먼저 정확히 복호할 수 있는 정보

- RAW: 이벤트를 그대로 출력
- 2×2/4×4 MASK: ON/OFF 비트맵을 해석해 픽셀 위치를 정확히 복원
- SYNC: 시간 기준값 갱신
- BIN: 위치는 알 수 없으므로 묶음별 ON/OFF 개수와 대표 시각만 기록

복호기는 각 시간 구간마다 다음 지도를 만든다.

```text
현재 ON/OFF 개수 지도
정확한 위치가 알려진 RAW/MASK 지도
정확한 위치와 BIN 위치를 구분하는 지도
2×2/4×4 비닝 크기 지도
마지막 정확 정보 이후 지난 시간 지도
대표 발생 시각 지도
```

#### 한 시점이 아니라 최근 여러 시점을 사용

현재 시간 구간을 `t`라고 하면 초기 모델은 `t-3`, `t-2`, `t-1`, `t`의 네 구간을 함께 본다. 한 클록 전에 RAW 또는 MASK가 들어왔다면 마지막 정확 정보 이후 지난 시간이 1인 표본으로 바로 사용할 수 있다. 이후에는 4개와 8개 구간을 비교한다.

```text
최근 K개 토큰 지도
        ↓
과거 특징을 현재 위치로 이동·정렬
        ↓
과거 상태를 기억하는 ConvGRU
        ↓
현재 시점의 ON/OFF 위치 확률
        ↓
정확 위치 고정 + BIN 개수 제약
        ↓
복원된 현재 이벤트
```

특징이 움직였을 수 있으므로 과거 지도를 같은 좌표에 그대로 겹치면 안 된다. 첫 구현은 작은 범위의 이동량을 비교해 가장 잘 맞는 위치로 옮긴다. 다음 단계에서는 작은 움직임 추정망이나 변형 가능한 합성곱을 비교한다. 정렬 신뢰도가 낮거나 새 물체가 나타난 경우에는 과거 정보의 가중치를 낮춘다.

실시간 사용을 기본으로 하므로 주 모델은 현재와 과거만 보는 인과형으로 만든다. 미래 구간까지 보는 양방향 모델은 실시간 회로와 직접 연결하지 않고, 복원 가능한 품질의 상한을 알아보기 위한 별도 비교 대상으로만 사용한다.

#### 모자이크 제거와의 관계

이 과정은 비어 있는 위치를 주변 정보로 채운다는 점에서 모자이크 제거와 비슷하다. 그러나 본 설계는 비닝 위치와 크기가 시간마다 달라지고 특징도 움직인다. 따라서 **시간에 따라 바뀌는 표본 위치를 움직임에 맞춰 합치는 적응형 이벤트 모자이크 제거**로 설명할 수 있다.

예를 들어 현재 4×4 BIN 하나만 보면 ON 이벤트 3개의 정확한 위치를 알 수 없다. 하지만 직전 구간에서 같은 영역이 네 개의 2×2 MASK로 전송되었고 윤곽이 오른쪽으로 한 픽셀 이동했다면, 그 이동을 반영한 위치가 강한 후보가 된다.

여기서 업스케일은 센서보다 더 높은 가상 해상도를 만드는 것이 아니라, 2×2 또는 4×4로 합쳐진 정보를 원래 센서의 픽셀 격자로 되돌리는 것을 뜻한다.

#### 이벤트 개수를 지키는 출력

BIN 토큰에 `ON=2`, `OFF=1`이 기록되어 있다면 소프트웨어도 해당 영역에서 ON 2개와 OFF 1개만 선택한다.

```text
1. 신경망이 픽셀별 ON/OFF 확률을 출력
2. RAW/MASK로 확인된 위치는 그대로 고정
3. BIN 영역 안에서 기록된 개수만큼 확률이 높은 위치를 선택
4. 2×2와 4×4 영역별 개수를 각각 확인
```

이 제약은 소프트웨어가 실제보다 많은 이벤트를 임의로 만들어내는 문제를 줄인다. 과거 정보는 위치의 우선순위를 정하는 단서로만 사용하고, 현재 토큰에 기록된 이벤트 개수와 극성은 바꾸지 않는다.

### 3.8 2차 과제와의 연결

두 가지 연결 경로를 정의한다.

#### 하드웨어만 사용하는 경로

```text
RAW/MASK 토큰
→ RTL 토큰 복호기
→ (x, y, 극성, 대표 시각)
→ 좌표 변환기
→ 월드 SRAM
```

#### 하드웨어-소프트웨어 평가 경로

```text
RAW/MASK/BIN 혼합 토큰
→ 최근 K개 시간 구간 구성
→ 움직임 정렬·시계열 소프트웨어 복원
→ 복원 이벤트
→ 소프트웨어 월드맵 기준 모델
→ 월드맵 오차 평가
```

1차 대회의 핵심 회로 기여는 하드웨어 부호기와 통신망에 둔다. 소프트웨어 복원은 손실이 단순 폐기가 아니라 전송량과 품질을 조절한 결과임을 보여주는 전체 검증 수단으로 사용한다.

### 3.9 차별점 설명 방법

차별점은 개별 기술 하나가 아니라 다음 결합으로 설명한다.

1. 고정형 풀링이 아니라 통신망 혼잡도에 따라 표현을 바꾼다.
2. 이벤트 밀도만 보지 않고 극성 일치도와 시간에 따른 패턴 변화도 함께 본다.
3. 4×4 수집 영역을 그대로 줄이거나 네 개의 2×2로 나누는 크기 선택을 하드웨어가 수행한다.
4. 중요한 고밀도 영역은 MASK로 보존하고 반복성이 높은 영역만 BIN으로 줄인다.
5. 마지막 정확 정보의 나이가 오래되면 MASK를 다시 보내 시계열 복원의 기준을 갱신한다.
6. 소프트웨어는 최근 여러 시점의 정확한 표본을 움직임에 맞춰 정렬한 뒤 현재 BIN을 복원한다.
7. 하드웨어 토큰이 소프트웨어 복원에 개수·극성·묶음 크기 제약을 제공한다.
8. RTL의 PPA와 소프트웨어 복원 품질을 하나의 전송량-오차 결과로 비교한다.

시계열 신경망이나 모자이크 제거 자체를 새로운 기술이라고 주장하면 약하다. 본 제안의 차별점은 **하드웨어가 혼잡도에 따라 표본의 정확도와 2×2/4×4 크기를 바꾸고, 마지막 정확 정보가 오래되면 기준 정보를 다시 보내며, 소프트웨어가 그 동적 표본 지도와 개수 제약을 이용해 움직임을 정렬한다는 닫힌 설계**에 있다. 다만 최종적인 신규성 주장은 관련 문헌을 더 조사한 뒤 확정한다.

---

## 4. 구현 방법

### 4.1 RTL 모듈 구성

권장 모듈 구조는 다음과 같다. 파일 이름은 구현할 때 그대로 사용할 수 있도록 영문 신호명으로 남긴다.

```text
rtl/
├─ care_aer_top.v
├─ event_input_sync.v
├─ pixel_event_latch.v
├─ microbin_4x4.v
├─ event_feature_estimator.v
├─ encoding_mode_controller.v
├─ event_packetizer_32b.v
├─ rv_fifo.v
├─ mesh_router_5p.v
├─ mesh_array.v
├─ gateway_arbiter.v
├─ token_decoder.v
├─ timestamp_sync.v
└─ care_aer_defs.vh
```

#### Verilog 작성 원칙

- 합성 대상 RTL은 Verilog-2001로 작성하고 파일 확장자는 `.v`를 사용한다.
- 신호는 `wire`와 `reg`, 순차 회로는 `always @(posedge clk)`, 조합 회로는 `always @(*)`로 작성한다.
- `logic`, `always_ff`, `always_comb`, `interface`, `package`, `typedef`, `enum`, `struct` 같은 SystemVerilog 문법은 사용하지 않는다.
- 모듈 경계의 여러 채널은 1차원 버스로 펼쳐서 전달한다. 예를 들어 네 개의 32비트 입력은 `[127:0]` 버스 하나로 묶는다.
- 공통 패킷 값과 상태 값은 `care_aer_defs.vh`의 `define` 또는 각 모듈의 `localparam`으로 선언한다.
- 필요한 비트 수 계산은 `$clog2`에 의존하지 않고 Verilog 함수 `clog2`를 직접 정의하거나 매개변수로 넘긴다.
- 크기는 `parameter`와 `generate`문으로 바꿀 수 있게 하되, 먼저 16×16 또는 32×32의 작은 배열로 기능을 검증한다.

#### `event_input_sync`: 이벤트 입력 동기화

- 비동기 픽셀 요청을 내부 클록 영역으로 전달
- 요청 신호가 저장될 때까지 1로 유지된다는 입출력 가정 사용
- 준안정 상태를 막기 위한 동기화 회로 또는 요청·응답 경계 정의

#### `pixel_event_latch`: 픽셀 이벤트 보관

- 아직 읽지 않은 이벤트와 극성 보존
- 저장 → 유지 → 읽기 → 초기화 → 다음 이벤트 대기 순서로 동작
- 한 시간 구간 안의 반복 이벤트 처리 방법과 넘침 상태 기록

#### `microbin_4x4`: 2×2/4×4 이벤트 묶음

- 4×4 픽셀의 16비트 ON/OFF 비트맵 생성
- 4×4를 네 개의 2×2 영역으로 나누어 각 4비트 비트맵도 동시에 생성
- 2×2별 개수와 4×4 전체 개수 계산
- 대표 시각 또는 가장 최근 시각 저장
- 이전 시간 구간의 비트맵 저장
- 영역별 마지막 정확 시간 구간 번호 저장과 활성 영역의 `exact_age` 계산

#### `event_feature_estimator`: 정보 특성 계산

- 1의 개수 계산을 이용한 이벤트 밀도
- ON/OFF 개수 비교를 이용한 극성 일치도
- 2×2 및 4×4 이전 비트맵과 현재 비트맵의 XOR를 이용한 패턴 변화량
- 반복 이벤트 또는 고립 이벤트 표시

#### `encoding_mode_controller`: 전송 형식 선택

- 이벤트 특성, 혼잡도, `exact_age`를 받아 RAW/MASK/BIN과 2×2/4×4 크기 결정
- 여유·보통·혼잡·매우 혼잡 상태에 히스테리시스 적용
- 최대 대기시간을 넘긴 이벤트의 우선순위 상승
- 4×4 BIN은 패턴 변화가 작고 최근 정확 정보가 있을 때만 허용

#### `event_packetizer_32b`: 32비트 패킷 생성

- RAW/MASK/BIN/SYNC 패킷 생성
- 4×4 MASK 머리부와 본문을 두 클록에 걸쳐 연속 출력
- 시간 차이와 기준 시각 관리
- 패킷 순서와 통계 계수기 관리

#### `mesh_router_5p`: 5방향 격자 라우터

- XY 경로 선택
- `ready/valid` 흐름 제어
- 순환 방식 출력 선택
- 장기 대기 방지용 계수기 또는 대기시간 비트
- 입출력별 정지 횟수와 FIFO 사용량 계수기
- 4×4 MASK 전송 중에는 머리부와 본문이 갈라지지 않도록 출력 선택을 한 번 더 유지

#### `token_decoder`: 토큰 복호기

- RAW, 2×2 MASK, 4×4 MASK, SYNC를 정해진 규칙으로 복호
- 4×4 MASK의 머리부와 본문 순서 및 번호 확인
- BIN 토큰은 소프트웨어로 보내거나 영역 요약 토큰으로 출력

### 4.2 핵심 상태 기계

타일 부호기는 다음 상태를 가진다.

```text
대기
  ↓ 이벤트 발생 또는 시간 구간 시작
수집
  ↓ 시간 구간 종료 또는 긴급 조건
분석
  ↓ 전송 형식 결정
RAW 전송 / 2×2 MASK 전송 / BIN 전송
  ↓ 4×4 MASK를 선택한 경우
4×4 MASK 머리부 전송 → 본문 전송
  ↓ FIFO 사용 가능
초기화
  ↓
대기
```

긴급 이벤트나 최대 대기시간에 가까운 이벤트는 시간 구간 종료를 기다리지 않고 RAW로 바로 보낼 수 있도록 우회 경로를 둔다.

### 4.3 발생 시각 처리

1. 전체 시간 계수기는 매 클록 증가한다.
2. SYNC 패킷이 기준 시각을 전송한다.
3. RAW/MASK/BIN은 기준 시각과의 차이 또는 시간 구간 번호를 전송한다.
4. 시간 차이 값이 표현 범위를 넘기 전에 SYNC를 강제로 보낸다.
5. MASK/BIN은 대표 시각을 사용하므로 원 이벤트 발생 시각과의 오차를 측정한다.

### 4.4 소프트웨어 구성

```text
sw/
├─ golden_encoder.py
├─ token_decoder.py
├─ build_sequence_dataset.py
├─ motion_align.py
├─ models/
│  ├─ single_frame_unet.py
│  ├─ temporal_convgru.py
│  └─ losses.py
├─ train.py
├─ infer.py
├─ event_metrics.py
├─ worldmap_metrics.py
└─ plot_rate_distortion.py
```

#### 학습 데이터 생성

```text
원본 고해상도 이벤트 흐름
        ├→ 정답 이벤트 지도
        └→ 소프트웨어 기준 부호기
              ↓
           RAW/MASK/BIN 토큰 흐름
              ↓
           최근 K개 시간 구간 구성
              ↓
           소프트웨어 모델 입력
```

소프트웨어 기준 부호기는 RTL과 동일한 기준값, 시간 구간, 최대값 제한, 패킷 형식을 사용한다.

#### 단일 시점 기준 모델

- 현재 BIN 토큰 한 구간만 입력
- 작은 U-Net으로 2×2/4×4 영역의 현재 위치 추정
- 시계열 방식의 효과를 확인하기 위한 비교 대상으로 사용

#### 제안하는 첫 시계열 모델

- 현재와 과거를 합쳐 최근 4개 시간 구간 입력
- ON/OFF를 나누어 출력
- 작은 특징 추출기와 ConvGRU로 과거 상태 유지
- 작은 범위의 이동량 탐색으로 과거 특징을 현재 위치에 정렬
- 정확 위치 지도, 비닝 크기 지도, `exact_age` 지도를 추가 입력으로 사용
- 이벤트 위치 오차 손실함수
- 이벤트 개수 보존 손실함수
- 극성 일치 손실함수
- 시간에 따른 윤곽 연속성과 잔상 억제 손실함수
- 영역별 개수만큼 확률이 높은 위치를 고르는 후처리

#### 후속 소프트웨어 모델

- 최근 8개 시간 구간으로 확대
- 학습형 움직임 정렬 또는 변형 가능한 합성곱 추가
- 시간 구간 안에서 클록 단위의 이벤트 발생 순서와 시각 차이 추정
- 미래 정보까지 보는 양방향 모델을 품질 상한으로만 비교

### 4.5 단계별 구현 순서

#### M0. 기준 모델과 사양 확정

- 패킷 비트 배치 확정
- RAW/MASK/BIN 부호화·복호화 파이썬 모델
- 이벤트 입력 생성기와 전송량-복원 오차 지표 구현

#### M1. 최소 부호기 구현

- 16×16 또는 32×32 입력
- 2×2 이벤트 묶음
- RAW/MASK/BIN/SYNC 패킷 생성기
- 단일 출력 FIFO
- 복호 결과 자동 비교

#### M2. 4×4 가변 비닝 추가

- 4×4 수집기에서 네 개의 2×2 결과도 함께 생성
- 2×2/4×4 BIN 개수 표현
- 4×4 MASK 머리부·본문 연속 전송
- `exact_age`와 강제 MASK 갱신

#### M3. 통신망 구현

- 2×2 격자망
- XY 경로, `ready/valid`, 순환 중재
- 지역 FIFO 혼잡도 되먹임
- 이후 통신 격자망을 4×4, 8×8 타일로 확장

#### M4. 적응형 선택 규칙

- 2×2/4×4 밀도·극성 일치도·패턴 변화량 계산
- 전송 형식 기준값과 히스테리시스
- 대기시간을 고려한 우선순위
- 마지막 정확 정보의 나이를 고려한 4×4 BIN 허용과 MASK 갱신
- 고정형과 적응형 비교

#### M5. 시계열 소프트웨어 복원

- 최근접·영역 중심·무작위 복원 기준 방식
- MASK 정확 복호
- 단일 시점 U-Net 기준 모델
- 움직임 정렬과 ConvGRU를 사용하는 인과형 시계열 모델
- 2×2/4×4 개수 제약과 정확 위치 고정
- RTL 토큰 기록을 직접 입력하는 연동 모의실험

#### M6. PPA 및 전체 흐름 평가

- Genus 논리 합성
- 가능하면 Innovus 배치·배선 및 배선 후 타이밍 분석
- 이벤트 입력에서 얻은 VCD/SAIF 기반 전력
- 전송량-복원 오차 및 월드맵 오차

### 4.6 범위 관리

핵심 제출 범위는 다음 다섯 가지로 제한한다.

1. 이벤트 정보 특성 계산기
2. 2×2/4×4를 선택하는 RAW/MASK/BIN 적응형 부호기
3. 최근 정확 정보의 나이에 따른 MASK 갱신 회로
4. 혼잡도 되먹임을 가진 타일 통신망
5. 현재와 과거를 비교하는 시계열 복원 검증 모델

1-4는 Verilog-2001로 구현하는 회로 기여이며, 5는 줄인 정보가 실제로 얼마나 복원되는지 확인하기 위한 소프트웨어 검증 수단이다.

다음 기능은 핵심이 완료된 후 추가한다.

- 패턴 코드표
- 확률적 이벤트 선택
- 라우터 내부 이벤트 병합
- 여러 출구
- 클록보다 세밀한 발생 시각을 추정하는 시간 초해상도 복원
- 미래 시간 구간까지 사용하는 비실시간 양방향 복원

---

## 5. 검증 방법

### 5.1 검증 단계

#### 모듈별 검증

- Verilog-2001 문법으로 전체 RTL이 분석·합성되는지 확인
- 4×4 ON/OFF 비트맵과 네 개의 2×2 비트맵 생성
- 2×2의 0-4, 4×4의 0-16 개수 계산
- 묶음 크기별 1의 개수, 패턴 변화량, 극성 일치도 계산
- 전송 형식 기준값과 히스테리시스
- 패킷 비트 배치의 부호화·복호화
- 4×4 MASK 머리부와 본문 생성·결합
- FIFO 가득 참·비어 있음·역압
- 라우터 XY 경로와 중재
- 시간 차이와 값 넘침

#### 타일 단위 검증

- 여러 픽셀의 동시 요청
- 이벤트 저장·유지·확인·초기화·재대기
- RAW/MASK/BIN과 2×2/4×4 전환
- 시간 구간 경계의 이벤트 처리
- 반복 이벤트 합치기와 통계 계수기
- 마지막 정확 시간 구간 번호의 갱신, `exact_age` 계산, 기준 초과 시 강제 MASK 갱신

#### 통신망 단위 검증

- 여러 타일의 동시 입력
- 집중 입력과 목적지 충돌
- 역압 전달
- 장기 대기와 대기시간 우선순위
- 교착 상태가 발생하지 않는지 확인
- 출구 출력 순서 확인
- 4×4 MASK 머리부와 본문 사이에 다른 패킷이 끼어들지 않는지 확인

#### 전체 흐름 검증

```text
입력 이벤트 기록
→ RTL 부호기와 통신망
→ 토큰 기록
→ 하드웨어·소프트웨어 복호기
→ 복원 이벤트 기록
→ 자동 정답 비교와 지표 계산
```

### 5.2 이벤트 입력 조건

공식 고정 입력 조건이 없으므로 아래 상황을 제안 기준으로 사용한다.

#### 입력률 변화

입력률과 기준 구조의 출력 능력 비율을 다음과 같이 정의한다.

\[
\rho=\frac{\lambda_{입력}}{\mu_{기준}}
\]

시험할 입력률 범위:

```text
ρ = 0.10, 0.25, 0.50, 0.75, 1.00, 1.25, 1.50, 2.00
```

#### 전체 영역 균일 확률 입력

- 전체 픽셀에 같은 발생 확률 적용
- 희소 입력부터 포화 상태까지 단계적으로 증가

#### 특정 영역 집중 입력

- 전체 면적의 25%가 이벤트의 80%를 발생시키는 조건
- 집중 영역을 가운데, 가장자리, 모서리로 변경

#### 순간 폭주

- 평상시 입력의 4배를 32, 64, 128클록 동안 인가
- FIFO가 폭주를 받아낸 뒤 정상적으로 비워지는지 확인

#### 움직이는 윤곽

- 수평·수직·대각선 윤곽이 이동
- 인접 픽셀에서 같은 극성 이벤트 생성
- 시간 구간마다 0, 1, 2, 4픽셀을 이동시키는 조건 비교
- 현재는 4×4 BIN이고 직전에는 RAW/MASK였던 경우 포함
- 움직임 정렬 전후의 윤곽 보존과 잔상 평가

#### 비닝 크기와 정확 형식 전환

- 같은 영역을 `MASK → 2×2 BIN → 4×4 BIN → MASK` 순서로 전환
- `exact_age`를 0, 1, 2, 4, 8로 바꾸어 복원 품질 측정
- 네 개의 2×2 중 일부만 복잡한 경우 4×4 전체를 BIN으로 줄이지 않는지 확인
- 새 물체 출현과 가려짐처럼 과거 정보가 오히려 방해되는 조건 포함

#### 복잡한 극성 패턴

- 바둑판 또는 ON/OFF 혼합 패턴
- 중요한 복잡 패턴이 BIN으로 지나치게 줄어들지 않는지 확인

#### 잡음과 반복

- 고립된 픽셀의 반복 이벤트
- 주변 픽셀도 함께 반응하는 실제 윤곽과 구분되는지 평가

#### 실제 센서 기록

- 실제 센서 이벤트와 촬영·회전 정보가 제공되면 같은 처리 흐름에 입력

### 5.3 기준 모델과 자동 검사 조건

필수 자동 검사 조건은 다음과 같다.

- FIFO가 넘치지 않았는데 패킷이 사라지지 않는다.
- RAW 복호 결과가 입력 `(x,y,p,t)`와 일치한다.
- 2×2 및 4×4 MASK의 ON/OFF 위치가 해당 시간 구간 입력과 일치한다.
- 4×4 MASK 머리부와 본문 번호가 맞지 않으면 오류를 검출한다.
- 2×2 및 4×4 BIN의 ON/OFF 개수가 토큰에 기록된 개수와 일치한다.
- SYNC 이후 복원된 발생 시각이 거꾸로 가지 않는다.
- 라우터가 잘못된 입출력으로 패킷을 보내지 않는다.
- `ready`가 0일 때 `valid` 데이터가 바뀌지 않는다.
- 입력률이 제한된 상황에서 최대 대기시간을 넘긴 패킷이 계속 방치되지 않는다.
- 같은 패킷이 두 번 출력되지 않는다.
- 격자망에 영구적인 교착 상태가 생기지 않는다.

### 5.4 비교할 기준 구조

#### B0. 전통적인 RAW AER

- 이벤트 하나당 32비트 패킷 하나
- 동일한 클록, 전체 FIFO 비트 수, 총 출력 대역폭 사용

#### B1. 고정형 MASK 그룹 AER

- 모든 2×2 묶음을 MASK 형식으로 전송
- 이벤트 입력 특성과 혼잡도를 고려하지 않음

#### B2. 고정형 2×2 BIN

- 모든 영역에 고정된 개수 요약 적용
- 전송량은 가장 적지만 공간 정보 손실이 큼

#### B3. 고정형 4×4 BIN

- 모든 영역에 4×4 개수 요약 적용
- 압축은 크지만 복잡한 윤곽과 새 물체의 위치 손실이 큼

#### P. 제안 CARE-AER

- RAW/MASK/BIN과 2×2/4×4 적응형 선택
- 이벤트 정보 특성과 혼잡도 되먹임
- 정확 정보 나이에 따른 MASK 갱신
- 이벤트 개수를 지키는 시계열 소프트웨어 복원

부호기의 효과와 통신망 구조의 효과가 섞이지 않도록 다음 비교를 나눈다.

1. 동일 통신망에서 B0/B1/B2/B3/P 패킷 정책 비교
2. 동일 패킷 정책에서 중앙 버스·트리·격자망 비교

### 5.5 평가 지표

#### 통신 성능

- 입력 이벤트 수
- 출력 패킷 수
- 출력이 나타내는 이벤트 수
- 이벤트당 전송 비트 수
- 압축률
- 지속 가능한 처리량
- 평균 지연
- 95·99백분위 지연과 최대 지연
- FIFO 사용량 분포
- FIFO 넘침 시점
- 의도적으로 정보를 줄인 비율
- 의도하지 않은 이벤트 손실률
- 타일별 공정성

#### 복원 성능

- 이벤트 정밀도·재현율·F1 점수
- 공간 좌표 평균 절대 오차
- ON/OFF 극성 정확도
- 발생 시각 평균 절대 오차
- 이벤트 개수 오차
- 이벤트 지도 겹침 비율
- 윤곽 연속성 또는 윤곽 보존 지표
- 시간에 따른 출력 흔들림과 잘못 남는 잔상
- 움직임 속도별 복원 성능
- 마지막 정확 정보 이후 지난 시간별 복원 성능
- 단일 시점 모델 대비 시계열 모델의 개선량

#### 2차 과제 연결

- 월드 좌표 평균 절대 오차
- 월드 이벤트 지도 겹침 비율
- 월드 메모리 셀의 극성 정확도
- 최근 발생 시각 오차

#### 회로 성능

- 셀 면적과 게이트 수
- 레지스터, FIFO, 메모리 비트 수
- 최대 클록 주파수
- 최악·전체 음의 타이밍 여유(WNS/TNS)
- 동적 전력과 누설 전력
- 이벤트당 또는 패킷당 에너지
- 라우터·부호기·복호기별 면적·전력 비중
- 배선량 또는 통신선 폭×이동 단계 수

### 5.6 공정한 비교 조건

모든 구조에 다음 조건을 동일하게 적용한다.

- 동일 센서 해상도
- 동일 이벤트 기록과 난수 시작값
- 동일 클록과 공정·전압·온도(PVT) 조건
- 동일 전송 단위 폭
- 동일 총 출력 대역폭
- 가능한 한 동일한 전체 FIFO 비트 수
- 동일 모의실험 길이와 초기 안정화 구간
- 동일 스위칭 활동도 추출 방식

여러 출구로 총 대역폭을 늘린 결과는 대역폭을 고정한 비교와 분리해 보고한다.

### 5.7 소프트웨어 학습 및 평가 분리

데이터는 학습·검증·시험 장면으로 나눈다. 같은 장면의 인접 시간 구간이 학습과 시험 데이터에 동시에 들어가 결과가 부풀려지지 않도록 한다.

비교 모델:

1. 영역 중심 배치
2. 최근접 위치 복원
3. 이벤트 개수를 지키는 무작위 배치
4. 현재 한 구간만 보는 U-Net
5. 정렬 없이 최근 구간을 단순히 쌓는 모델
6. 움직임 정렬 없이 ConvGRU만 사용하는 모델
7. 제안하는 움직임 정렬·ON/OFF 분리·개수 제약형 인과 모델
8. 미래 구간까지 보는 양방향 모델. 실시간 방식의 비교 상한으로만 사용

하드웨어 전송 정책별로 모델을 따로 학습하는 방법과, 전송 형식·묶음 크기·마지막 정확 정보의 나이를 입력받는 하나의 통합 모델을 비교한다. 같은 장면의 이어진 구간 전체를 하나의 묶음으로 나눈 뒤 학습·검증·시험에 배정해 시간적으로 인접한 자료가 서로 섞이지 않도록 한다.

### 5.8 성공 판정 기준

기준 구조의 결과를 얻기 전에 임의의 개선률을 약속하지 않는다. 대신 다음 조건을 통과해야 한다.

1. RAW는 입력 이벤트를 비트 단위로 정확히 복원한다.
2. MASK는 같은 시간 구간의 ON/OFF 위치 비트맵을 정확히 복원한다.
3. 2×2와 4×4 BIN은 각각 ON/OFF 이벤트 개수를 보존한다.
4. 의도적인 정보 축약을 끈 상태에서는 측정된 안전 입력률 이하에서 이벤트 손실이 0%여야 한다.
5. 같은 전송량에서 제안 구조가 고정형 BIN보다 복원 F1 또는 월드맵 오차가 좋아야 한다.
6. 같은 복원 품질에서 제안 구조가 RAW 또는 고정형 MASK보다 이벤트당 전송 비트를 줄여야 한다.
7. 적응형 제어 회로의 면적·전력 비용을 포함하고도 이벤트당 에너지 또는 처리량 이득을 보여야 한다.
8. 희소·집중·순간 폭주 중 한 조건에서만 좋고 다른 조건에서 크게 나쁘다면 적용 범위와 원인을 밝혀야 한다.
9. 같은 토큰과 전송량에서 시계열 모델이 단일 시점 모델보다 움직이는 윤곽의 복원 F1 또는 좌표 오차를 개선해야 한다.
10. 과거 정보가 없는 새 물체나 가려짐 조건에서 심한 잔상이 생기면 정렬 신뢰도 제한 또는 MASK 갱신으로 줄여야 한다.

### 5.9 주요 위험과 대응

| 위험 | 영향 | 대응 |
|---|---|---|
| 이벤트 수를 중요도로 잘못 판단 | 중요한 집중 영역 손실 | 패턴 변화·극성·대기시간을 함께 사용하고 MASK 우선 |
| BIN에서 위치를 정확히 알 수 없음 | 이벤트 지도 오차 | 개수 제약, 마지막 정확 정보의 나이, 주기적 RAW/MASK 기준 정보 |
| 4×4 BIN이 중요한 세부 정보를 지나치게 합침 | 윤곽·작은 물체 손실 | 패턴 변화가 작고 최근 정확 정보가 있을 때만 허용하고 아니면 2×2로 분할 |
| 4×4 MASK가 두 전송 단위를 사용 | 지연·라우터 제어 증가 | 이벤트 3개 이상에서만 사용하고 머리부 뒤 본문까지 출력 잠금 |
| 과거 특징의 이동을 잘못 맞춤 | 잘못된 위치와 잔상 | 정렬 신뢰도가 낮으면 과거 가중치를 낮추고 새 MASK 기준 정보 사용 |
| 출구 하나인 격자망의 혼잡 | 확장성 제한 | 대역폭 고정 비교를 정직하게 제시하고 여러 출구는 별도 분석 |
| 정보 특성 계산 회로의 비용 | PPA 이득 상쇄 | 4×4 비트맵에서 2×2 중간 결과를 재사용하고 블록별 비용 공개 |
| 전송 형식이 자주 바뀜 | 지연·전력 증가 | 히스테리시스와 최소 유지시간 적용 |
| 대표 시각 사용에 따른 오차 | 2차 좌표 변환 오차 | 대표 시각과 최대 시간 차이를 보고하고 RAW 우회 경로 사용 |
| 소프트웨어가 하드웨어 기여를 가림 | 대회 적합성 약화 | RTL 부호기·통신망을 핵심 제출물로 유지 |
| 소프트웨어가 잘못된 이벤트를 생성 | 복원 신뢰도 저하 | ON/OFF 개수 제약과 신뢰도 출력 사용 |

### 5.10 최종 산출물

- 구조 및 패킷 사양서
- 파이썬 기준 부호기·복호기
- 매개변수화한 Verilog-2001 RTL
- 자동 정답 검사가 가능한 모듈·타일·통신망 시험 환경
- 이벤트 입력 생성기와 실제 기록 변환기
- RTL 토큰 기록을 사용하는 인과형 시계열 소프트웨어 복원 모델
- Genus 합성 보고서
- 가능하면 Innovus 배치·배선, 배선 후 타이밍, VCD/SAIF 전력 보고서
- 기준 구조 대비 처리량·지연·손실·PPA 표
- 전송량-복원 오차 그래프
- 복원 및 월드맵 정확도 결과
- 2차 좌표 변환기 입출력 사양

---

[^qa]: [1차 Q&A 정리](./1차_Q&A_정리.md). 공식 확정 조건과 팀이 정해야 하는 조건을 구분한 검색용 문서.
[^bg-summary]: [AER 관련 논문 정리](<./Background/AER 관련 논문 정리.pdf>). 기준 AER, 연속 전송, 그룹 AER, 계층형·설정 가능 구조를 학습하고 비교하기 위한 자료.
[^esr-2021]: Siqi Li et al., [Event Stream Super-Resolution via Spatiotemporal Constraint Learning](https://openaccess.thecvf.com/content/ICCV2021/papers/Li_Event_Stream_Super-Resolution_via_Spatiotemporal_Constraint_Learning_ICCV_2021_paper.pdf), ICCV 2021.
[^bmcnet-2024]: Zhilin Huang et al., [Bilateral Event Mining and Complementary for Event Stream Super-Resolution](https://cvpr.thecvf.com/virtual/2024/poster/30319), CVPR 2024.
